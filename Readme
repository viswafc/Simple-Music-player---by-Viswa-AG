1)Remove or disable antivirus before useing the application  
                                    (or)
2)To run the python code install the following libraries using the command in cmd:
                            >>>     pip install pygame mutagen tkinterdnd2
-Viswa AG 
